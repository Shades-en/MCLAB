			AREA SUMDIGIT, CODE, READONLY
DIVIDEND	RN 4
DIVISOR		RN 5
QUOTIENT	RN 3
REMAINDER	RN 2
SUM			RN 0
ENTRY
		LDR R1, =12345
		MOV DIVISOR, #10
		MOV DIVIDEND, R1
		MOV SUM, #0
LOOP	BL LOOP2
		ADD SUM, SUM, REMAINDER
		MOV DIVIDEND, QUOTIENT
		CMP QUOTIENT, #0
		BNE LOOP
STOP	B STOP
LOOP2	MOV QUOTIENT, #0
DIV		SUBS DIVIDEND, DIVIDEND, DIVISOR
		ADDPL QUOTIENT, QUOTIENT, #1
		BPL DIV
		ADDMI REMAINDER, DIVIDEND, DIVISOR
		BX LR
		END

		
		